#ifndef __GUARD_SPLIT_H_
#define __GUARD_SPLIT_H_

#include <vector>
#include <string>
#include <iostream>
#include <cctype> // c style의 기능을 cpp에서 상속 받은 것.

std::vector<std::string> split(const std::string& );

#endif
